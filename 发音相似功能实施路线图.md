# 单词匹配功能实施路线图

## 1. 目标与范围

在现有单词发现和手动查找功能中，增加基于音素的发音匹配，并提供统一的匹配类型选择：

- **相似**：沿用现有拼写相似判断。
- **换序**：沿用现有换序判断；实现前确认现有代码对字母换序的定义，不擅自扩大为短语词序交换。
- **发音**：优先使用 CMU Pronouncing Dictionary（CMUdict）的音素序列计算相似度，逐步加入词典外单词的 G2P（拼写转音素）预测。

适用入口：自动发现、手动查找。第一版以英语单词、美式读音为范围。英式读音、依赖上下文的多音词消歧，以及短语连读和弱读不作为首版承诺。

本文为实施计划，复选框表示尚待完成的工作，并不表示功能已经实现。本轮本地命令环境无法启动，未核实仓库文件结构；具体模块位置和现有换序语义需在第一阶段确认。

## 2. 用户选择规则

### 2.1 默认状态与约束

初次进入时显示三个复选框，默认全部勾选：

```text
匹配类型  [✓] 相似  [✓] 换序  [✓] 发音
至少选择一项
```

- 允许任意单选、双选或全选，共七种有效组合。
- 已选两项或三项时，可以取消其中任意一项。
- 仅剩一项时，不能取消最后一项；保持选中，并提示“至少选择一种匹配类型”。
- 鼠标、触屏和键盘操作均遵守同一约束；提示应能被辅助技术读取。
- 当前会话内切换两个入口时保留选择，两处控件保持同步。
- 首版不做跨会话持久化，新会话默认全选，避免旧设置改变默认体验。

### 2.2 匹配语义

多个选项采用 **OR（并集）**：满足任意已选类型即可进入结果，无需同时满足所有类型。

| 选择 | 结果范围 |
| --- | --- |
| 相似 | 仅拼写相似 |
| 换序 | 仅换序匹配 |
| 发音 | 仅发音相似 |
| 相似、换序 | 两类匹配的并集 |
| 相似、发音 | 两类匹配的并集 |
| 换序、发音 | 两类匹配的并集 |
| 相似、换序、发音 | 三类匹配的并集 |

- 同一单词只展示一次，标注它命中的已选类型。
- 取消一类后，仍命中其他已选类型的单词保留。
- 未启用的类型不参与候选召回，不触发对应的额外计算。
- 改选类型后重置分页并重新计算结果；丢弃旧请求的迟到响应，防止结果回跳。
- 无结果时明确提示当前选择下没有匹配项，不自动改回全选。

### 2.3 排序与分数展示

- 拼写分数和发音分数独立展示，不将两个分数直接相加，也不宣称分数是正确概率。
- 仅选择发音时，按发音相似度降序排列，并使用稳定的并列排序规则。
- 包含相似或换序时，保留现有对应结果的相对顺序；新增的仅发音命中项追加在后，按发音分数排序。
- 同时命中多个类型的结果合并标签，不重复插入，也不额外增加排名权重。
- 发音来源分别标注“词典读音”“预测读音”；无可用读音时标为“暂无读音”，不能当作零分。
- Soundex 不能标为词典音素匹配。迁移完成后退出正式发音评分，必要时仅用于内部对照。

## 3. 推荐技术方案

### 3.1 优先采用预计算和浏览器计算

推荐首版在构建期或数据更新阶段预处理词表，Cloudflare 分发版本化静态数据，浏览器通过 Web Worker 执行候选查找和音素比较。

```text
构建或数据更新
  业务词表 → 文本规范化 → 查询 CMUdict → 保存全部读音变体
                                      → 缺失词记录 → 后续 G2P 预计算
  音素数据 → 建立候选索引 → 输出版本化静态资源

用户查询
  输入 + 已选类型 → 分别召回候选 → 各自评分和阈值过滤
                → 取并集 → 去重 → 排序 → 展示命中类型
```

这样可减少 Workers 请求和 CPU 消耗。若现有实现要求服务端匹配，则复用相同算法模块，在 Worker 内仅比较数量受限的候选，并通过线上 CPU 测量确认是否满足免费层限制。

用户私人词表不得打包进公共静态资源。共享 CMUdict 数据可公开分发；个人词表、个人索引继续沿用项目现有的隔离和鉴权方式。

### 3.2 发音数据与评分

- 固定 CMUdict 版本，保留来源、许可及版权声明。
- 统一处理大小写、空白、撇号和连字符，避免查询键不一致。
- 保存单词全部读音变体，不仅保留第一项。
- 使用音素 token 序列计算编辑距离，不对 ARPAbet 字符串逐字符计算。
- 基础分数：`1 - distance / max(lengthA, lengthB)`；仅对有效、非空音素序列计算。
- 首版采用单位编辑代价；基础音素与重音信息分开保存，重音差异可独立展示。
- 多读音比较全部变体组合，采用最高相似度，并保留对应读音对以便解释结果。
- 后续通过验证集决定元音、辅音、清浊和重音的权重；加权距离需要重新设计归一化，不能直接沿用单位代价公式。
- 评分版本、词典版本、索引版本应明确记录，避免旧缓存与新算法混用。

### 3.3 候选召回

- 发音候选独立召回，不得只给拼写候选补算发音分数，否则会漏掉拼写差异很大的同音词。
- 初始业务词表规模约 7,214 条，仅为截图中的接口总数，完整同步后需重新确认。
- 浏览器全量比较可作为正确性基线；是否需要索引由性能测量决定。
- 如需索引，可组合音素长度范围、音素片段索引和同音词映射；避免仅按首字母或首音素筛选。
- 使用全量比较结果验证索引召回率，不能只验证运行速度。
- 不在每次 Worker 请求中扫描整个 CMUdict，也不将数据库全表扫描作为查询方案。

### 3.4 G2P 分阶段接入

1. 首版先完成 CMUdict 路径，词典外词明确显示暂无读音。
2. 统计业务词表实际缺失率，评估需要覆盖的生词、专有名词和拼写错误。
3. 对已知缺失词在本地或构建环境运行 G2P，校验输出音素格式后生成静态补充数据。
4. 对用户临时输入的未知词，单独评估浏览器轻量模型或预测服务；上线前验证准确性、包体积、延迟与费用。
5. 预测结果保留模型版本和来源，缓存重复查询；模型失败时保留其他已选类型的结果，并提示发音部分不可用。

不把规则式 G2P 视为与神经模型或词典同等可靠的替代品。不承诺任意生词的即时预测一定能在免费 Workers 中运行。

## 4. 分阶段任务与完成标准

### 阶段一：确认数据和现有行为

- [x] 定位自动发现、手动查找、Soundex、拼写匹配、换序匹配及排序入口。
- [x] 明确现有换序定义，整理需保持行为不变的样例。
- [x] 检查当前同步提示“日期分区计数不一致”的触发条件。
- [x] 确认完整词表覆盖情况，不能用已同步的 27 条代表全部 7,214 条。
- [x] 统计规范化后的有效单词数、重复词、短语、非英语项及 CMUdict 缺失率。
- [x] 建立代表性样例集，包含同音词、近音词、拼写相近但读音不同、多音词和词典外词。

阶段一结果见 [`docs/pronunciation-phase-1-report.md`](docs/pronunciation-phase-1-report.md)。当前快照为 7,140 / 7,154 条的部分覆盖，后续开发不得宣称已覆盖完整用户数据。

完成标准：明确输入数据范围、现有匹配语义及覆盖缺口。同步不完整时可以使用独立测试词表开发，但不得宣称已覆盖全部用户数据。

### 阶段二：完成三选项交互

- [x] 增加“相似 / 换序 / 发音”复选框，首次进入默认全选。
- [x] 实现至少选一项的状态约束及提示。
- [x] 自动发现与手动查找共用选择状态。
- [x] 定义查询参数 `matchTypes`，内部值为 `spelling / reorder / pronunciation`。
- [x] 请求未提供该参数时默认全选，以兼容旧调用；显式空数组或非法类型返回参数错误。
- [x] 两个入口均在 Web Worker 统一入口执行同等校验。
- [x] 完成 OR 合并、去重、类型标签、分页重置和迟到响应防护。

完成标准：七种选择组合均可用，前后端或本地查询入口均不会接受空选择。交互可先在开发环境完成，正式启用“发音”选项须等待阶段三通过。

### 阶段三：上线词典音素匹配 MVP

- [x] 引入固定版本 CMUdict，构建数据转换流程并保留许可声明。
- [x] 输出业务词表的全部读音变体和缺失清单。
- [x] 实现音素 token 编辑距离、归一化及多读音比较。
- [x] 建立独立发音候选路径，接入两个入口。
- [x] 实现排序规则和来源展示，替换正式结果中的 Soundex 分数。
- [x] 验证浏览器主线程不会因比较计算明显卡顿，计算已放入 Web Worker。
- [x] 检查静态资源体积、首次加载、缓存与版本更新行为。

完成标准：同音而拼写差异大的词能够通过发音入口找到；词典缺失不会被误报为“不相似”；关闭发音后不再执行该路径。

阶段二、三于 2026-09-22 完成。CMUdict 固定为提交 `74790861f652b15e4ac49015a90074ad62a27690`，源文件 SHA-256 为 `81917843c7f44ce2b094ac63873c2c7a4cf802040792c455ba3ca406891c3d22`。生成资源含 126,052 个词条、大小 5,487,892 字节，使用版本化路径和一年 `immutable` 浏览器缓存；许可证随部署产物分发。当前私有快照口径仍为 6,823 个唯一英文单词，其中 6,739 个有词典读音、84 个缺失。自动发现的发音阈值与界面阈值一致；手动纯发音 MVP 阈值暂定 0.65，等待阶段五校准。1,000 词、499,500 对的三路径合成基准耗时约 1.29 秒，计算位于 Web Worker；浏览器回归覆盖默认全选、至少一项、双入口同步及词典读音展示。Soundex 代码暂保留给旧输入单元测试，但已退出正式结果展示和评分。

### 阶段四：补齐词典外词

- [x] 根据缺失清单选择并验证 G2P 模型及许可。
- [x] 在构建期生成已知词典外词的预测读音。
- [x] 区分词典结果与预测结果，保证音素符号和重音格式一致。
- [x] 验证模型失败、资源加载失败及缓存失效时的行为。
- [x] 评估实时预测条件：本阶段仅处理已知缺失词，任意新词实时预测不启用；未来确有需求时另行完成部署验证。

完成标准：已知缺失词有可追溯的预测来源；未支持的实时未知词如实提示，不返回伪造读音。

阶段四于 2026-09-22 完成本地实现与验收，详见 [`docs/pronunciation-phase-4-report.md`](docs/pronunciation-phase-4-report.md)。固定 `g2p_en 2.1.0 / checkpoint20`（Apache-2.0），84 个已知缺失单词全部生成格式有效的预测读音。私人补充文件只在本机生成，通过页面导入供两个入口共享，不打包到公共资源。预测可用不等于人工确认准确；当前快照仍为部分覆盖，正式发布留到阶段五。

### 阶段五：质量调优、验收与发布

- [ ] 建立约 100～200 对人工核验样例，覆盖正例和容易误判的负例。（160 对草案及核验表已建立，人工核验待完成。）
- [x] 将调参样例与最终验收样例分开，分别检查误报和漏报。
- [x] 校准发音阈值，必要时区分短词和长词；记录评分版本。（草案评测保留 0.65，最终人工验收待完成。）
- [x] 评估是否加入音系特征权重和重音惩罚。
- [x] 用全量基线验证候选索引不会造成不可接受的漏召回。（保留全量比较，独立基线在 50,721 对、四档阈值下结果一致。）
- [x] 回归七种组合、双入口、去重、排序、空结果和最后一项不可取消。
- [x] 验证词表更新、同步中断、旧缓存、断网及发音资源失败场景。
- [ ] 记录真实数据规模下的延迟、CPU、资源体积及 Cloudflare 实际消耗。（本机真实快照及浏览器测量完成，线上消耗待测。）
- [ ] 保留旧资源版本和可回滚发布产物，通过项目既有流程发布。（旧资源与新旧本地产物已保留，正式发布待完成。）

阶段五于 2026-09-22 完成本地交付，用户确认“采用这些目标，先交付 localhost”。验收目标为误报率/漏报率各不超过 10%，1,000 词自动发现不超过 5 秒，全快照 ×20 查询不超过 3 秒。草案标签评测及本机性能满足这些目标；人工标签核验和 Cloudflare 线上发布未冒充完成。详见 [`docs/pronunciation-phase-5-report.md`](docs/pronunciation-phase-5-report.md)。

完成标准：在独立验收集上达到发布前约定的误报、漏报和性能标准；未达标时记录具体失败样例，不使用未经验证的准确率宣传。

## 5. Cloudflare 免费层预算

以下是截至 2026-09-21 已查阅的官方额度，实施部署前应再次核对。额度由账户下相关服务共享，词条数量不能直接等同请求数或数据库读写量。

| 服务 | 免费额度 | 本功能的使用原则 |
| --- | --- | --- |
| Workers | 每天 100,000 次请求；每次 10ms CPU；128MB 内存 | 只做轻量查询和有限候选比较，实际测量 CPU |
| Workers 静态资源 | 静态资源请求免费且不限量；需遵守资源大小等限制 | 优先分发版本化词典分片和音素索引，核实路由不会先运行动态 Worker |
| D1 | 每天读 500 万行、写 10 万行；总存储 5GB | 若使用，按单词建立索引；避免查询时全表扫描，考虑索引写入开销 |
| KV | 每天读取 100,000 个键、写入 1,000 个键；1GB | 可选缓存，不将十几万词条逐词导入视为一次写入 |

首选静态预处理方案，无需为本功能强制新增 D1 或 KV。免费层可行性取决于流量、现有账户用量和实测性能，不能仅由 7,214 条数据保证。完整神经 G2P 的实时部署必须单独验证。

参考：

- [Workers 定价](https://developers.cloudflare.com/workers/platform/pricing/)
- [Workers 限制](https://developers.cloudflare.com/workers/platform/limits/)
- [D1 定价](https://developers.cloudflare.com/d1/platform/pricing/)
- [KV 定价](https://developers.cloudflare.com/kv/platform/pricing/)
- [CMUdict 官方仓库](https://github.com/cmusphinx/cmudict)
- [CMUdict 格式与重音说明](https://github.com/cmusphinx/cmudict-tools)

## 6. 工期与交付物

以一名熟悉项目的开发者为前提，暂估：

| 里程碑 | 预计工作量 | 交付物 |
| --- | --- | --- |
| 现状确认与样例整理 | 0.5～1 天 | 现有行为说明、数据覆盖统计、初始验证样例 |
| 三选项交互与查询接入 | 1～2 天 | 两个入口统一控件、参数校验、七种组合支持 |
| CMUdict MVP | 2～3 天 | 音素数据、比较算法、候选召回、来源展示 |
| 构建期 G2P 补齐 | 1～3 天 | 模型构建流程、缺失词补充数据、来源标注 |
| 质量与性能验收 | 1～3 天 | 验收记录、性能预算、可回滚发布产物 |

阶段一至三的词典版 MVP 预计约 4～6 个工作日；含构建期 G2P 和系统验收约 6～12 个工作日。同步故障修复及任意新词实时 G2P 的工作量尚未计入，需完成代码检查后估算。

最终用户验收要点：默认三个选项全勾，至少保留一项；所选类型取并集；发音结果可独立找到，来源可辨认；现有相似与换序行为通过回归验证。
